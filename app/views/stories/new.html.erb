<div class="max-w-sm lg:max-w-md">
  <h1 class="font-bold text-4xl mb-10">🔮 Nova história 🔮</h1>
  <%= form_with model: @story do |form| %>
    <div class="mb-5">
      <%= form.label :age, "Idade" %>
      <%= form.select :age, options_for_select((2..10).to_a, params[:age] || 4), { include_hidden: false }, class: "dark:bg-slate-800 rounded dark:border-white w-full" %>
    </div>
    <div class="mb-5">
      <%= form.label :keywords, "Palavras-chave" %>
      <%= form.select :keywords, @keywords, { include_hidden: false }, class: "dark:bg-slate-800 rounded dark:border-white w-full", multiple: true, max: 3 %>
    </div>
    <div>
      <%= form.submit "Criar", class: "dark:bg-cyan-700 p-2 cursor-pointer rounded w-full" %>
    </div>
  <% end %>
</div>
