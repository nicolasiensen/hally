<div class="max-w-sm lg:max-w-md">
  <div class="mb-5">
    <% @story.keywords.each do |keyword| %>
      <span class="dark:bg-indigo-700 inline-block px-2 rounded-full"><%= keyword %></span>
    <% end %>
  </div>
  <div class="text-2xl mb-10">
    <%= simple_format(@story.body, class: "mb-5") %>
  </div>
    <%= link_to "Nova história", new_story_path, class: "dark:bg-cyan-700 p-2 cursor-pointer rounded" %>
  </div>
</div>